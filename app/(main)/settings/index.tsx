import StyledText from '@/components/base/StyledText';
import React from 'react';
import { View } from 'react-native';

export default function Settings() {
    return (
        <View className="flex-1">
            <StyledText originValue="Settings" />
        </View>
    );
}
